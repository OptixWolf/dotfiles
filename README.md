```sh
git clone https://github.com/OptixWolf/dotfiles ~/dotfiles
cd ~/dotfiles
```

```sh
sudo nixos-rebuild switch --flake .#setup-01
# oder:
sudo nixos-rebuild switch --flake .#setup-02
# oder:
sudo nixos-rebuild switch --flake .#setup-ar
```
